## 一、 基础配置与初始化

| 命令 | 说明 |
| --- | --- |
| `git config --global user.name "你的名字"` | 设置全局用户名 |
| `git config --global user.all "你的邮箱"` | 设置全局邮箱 |
| `git init` | 在当前文件夹初始化一个新的本地仓库 |
| `git clone <url>` | 从远程服务器克隆（下载）一个现有的仓库 |

---

## 二、 基础工作流：修改与提交

| 命令 | 说明 |
| --- | --- |
| **`git status`** | **最常用！** 查看当前工作区状态（哪些文件改了、哪些还没提交） |
| `git add <文件名>` | 将指定文件添加到暂存区（Staging Area） |
| `git add .` | 将当前目录下所有修改添加到暂存区 |
| `git commit -m "提交消息"` | 将暂存区的内容提交到本地仓库 |
| `git commit --amend` | 修改上一次的提交消息，或合并漏掉的文件到上次提交 |

---

## 三、 分支管理：代码的多宇宙

| 命令 | 说明 |
| --- | --- |
| `git branch` | 列出所有本地分支，当前分支前会有 `*` 号 |
| `git branch <分支名>` | 创建一个新分支，但停留在当前分支 |
| **`git switch <分支名>`** | **切换分支**（现代 Git 推荐，比 `checkout` 更专一） |
| `git switch -c <分支名>` | 创建并立即切换到新分支 |
| `git merge <分支名>` | 将指定分支的代码合并到当前分支 |
| `git branch -d <分支名>` | 删除已合并的分支 |

---

## 四、 远程同步

| 命令 | 说明 |
| --- | --- |
| `git remote -v` | 查看远程仓库详细地址 |
| `git fetch` | 从远程拉取最新代码，但不自动合并 |
| **`git pull`** | 拉取并自动合并（相当于 `fetch` + `merge`） |
| **`git push`** | 将本地提交推送到远程仓库 |
| `git push -u origin <分支名>` | 推送新分支并建立关联，以后只需 `git push` |

---

## 五、 查看历史与差异

| 命令 | 说明 |
| --- | --- |
| `git log` | 查看提交历史（按 `q` 退出） |
| `git log --oneline` | 以精简的一行模式查看历史 |
| `git diff` | 查看工作区和暂存区的代码差异 |
| `git diff HEAD` | 查看工作区和上一次提交之间的差异 |

---

## 六、 救急与撤销：后悔药系列

| 命令 | 说明 | 适用场景 |
| --- | --- | --- |
| **`git checkout -- <文件>`** | 丢弃工作区的修改 | 还没 `add`，代码写乱了想重来。 |
| **`git reset HEAD <文件>`** | 取消暂存 | 已经 `add` 了，但还没 `commit`。 |
| **`git reset --hard HEAD^`** | **回退版本** | 已经 `commit` 了，回退到上一个版本（代码会丢！）。 |
| `git revert <commit-id>` | 撤销某次提交 | 已经 `push` 了，通过新提交来抵消旧提交。 |
| **`git stash`** | **代码暂存** | 代码写一半，突然要切分支修 Bug，先存起来。 |
| `git stash pop` | 恢复暂存代码 | Bug 修完回来，把存的代码拿出来继续写。 |

---
